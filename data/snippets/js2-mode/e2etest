# -*- mode: snippet -*-
# name: e2etest
# key: e2etest_
# --
import * as utils from '../utils'

const timeout = 32000

describe('$1', () => {
  it('$2', async () => {
    let page = await goto$3

    await expect(await page.title()).toEqual('Case Management | $4')

    $0
  }, timeout)
})
